# Server Configuration
HOST=0.0.0.0
PORT=8080

# Railway PostgreSQL Database Configuration
# Primary connection (internal network - use for production deployment on Railway)
DATABASE_URL=postgresql://postgres:your_password@postgres.railway.internal:5432/railway

# Railway Database Variables (for reference)
PGUSER=postgres
PGPASSWORD=your_password
PGDATABASE=railway
PGPORT=5432
PGHOST=postgres.railway.internal

# Railway proxy connection (for external/local development access)
RAILWAY_PRIVATE_DOMAIN=postgres.railway.internal
RAILWAY_TCP_PROXY_DOMAIN=your_proxy.proxy.rlwy.net
RAILWAY_TCP_PROXY_PORT=your_port
DATABASE_PUBLIC_URL=postgresql://postgres:your_password@your_proxy.proxy.rlwy.net:your_port/railway

# For local development, use DATABASE_PUBLIC_URL instead:
# DATABASE_URL=postgresql://postgres:your_password@your_proxy.proxy.rlwy.net:your_port/railway

# SMTP Configuration (SendGrid/Resend/Gmail/Yahoo)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USERNAME=apikey
SMTP_PASSWORD=your_sendgrid_api_key
SMTP_FROM=autoassist.contact@yahoo.com
ADMIN_EMAIL=autoassist.contact@yahoo.com

# For Yahoo Mail:
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=465
# SMTP_USERNAME=your-email@yahoo.com
# SMTP_PASSWORD=your-app-password

# For Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# For Resend:
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=587
# SMTP_USERNAME=resend
# SMTP_PASSWORD=re_your_api_key

# Logging
RUST_LOG=autoassist_api=info,tower_http=info
